"use server";

export async function addToCart(productId: string, quantity: number) {
  // Aquí tu lógica: session/userId, escribir en BD/Redis/cookie, etc.
  // await db.cart.add({ userId, productId, quantity })
}
